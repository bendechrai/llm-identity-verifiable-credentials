# Required for Ralph (autonomous Claude Code runner)
CLAUDE_CODE_OAUTH_TOKEN=your_claude_code_oauth_token

# Optional: For git push after each iteration
GITHUB_TOKEN=your_github_token

# Optional: For LLM Agent modes other than mock
LLM_MODE=mock
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
