# Environment Configuration for LLM Identity Demo
# Copy this file to .env and fill in values as needed

# ==============================================================================
# Ralph (Autonomous Claude Code Runner)
# ==============================================================================
# Required for Ralph service to operate
CLAUDE_CODE_OAUTH_TOKEN=your_claude_code_oauth_token

# Optional: For git push after each iteration
GITHUB_TOKEN=your_github_token

# ==============================================================================
# LLM Agent Configuration
# ==============================================================================
# LLM_MODE controls how the agent processes natural language:
#   mock     - Scripted responses for deterministic demos (default, recommended)
#   ollama   - Local Ollama instance (requires Ollama running at localhost:11434)
#   openai   - OpenAI API (requires OPENAI_API_KEY)
#   anthropic - Anthropic API (requires ANTHROPIC_API_KEY)
LLM_MODE=mock

# API keys for cloud LLM providers (only needed if using those modes)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ==============================================================================
# Service Ports (for reference - these are set in docker-compose.yaml)
# ==============================================================================
# demo-ui:     3000  - Web presentation interface
# vc-issuer:   3001  - Issues EmployeeCredential + FinanceApproverCredential
# vc-wallet:   3002  - Stores credentials, creates Verifiable Presentations
# auth-server: 3003  - Verifies VPs, issues constrained JWTs (trust boundary)
# llm-agent:   3004  - Orchestrates authorization flow
# expense-api: 3005  - Resource server, validates JWTs, enforces ceiling

# ==============================================================================
# Quick Start
# ==============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Start all services: docker-compose up
# 3. Open http://localhost:3000 for the demo UI
# 4. Or use npm test to run unit tests without Docker
